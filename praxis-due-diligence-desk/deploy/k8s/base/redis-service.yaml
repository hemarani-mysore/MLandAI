apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: praxis
  labels:
    app: praxis
    component: redis
spec:
  selector:
    app: praxis
    component: redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis
