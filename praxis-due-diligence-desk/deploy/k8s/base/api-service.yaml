apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: praxis
  labels:
    app: praxis
    component: api
spec:
  selector:
    app: praxis
    component: api
  ports:
    - name: http
      port: 8000
      targetPort: http
