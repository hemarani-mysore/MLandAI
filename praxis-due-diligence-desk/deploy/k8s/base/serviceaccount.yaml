apiVersion: v1
kind: ServiceAccount
metadata:
  name: praxis
  namespace: praxis
  # automountServiceAccountToken: false — nothing here talks to the k8s API,
  # so the default token isn't needed and stays unmounted.
automountServiceAccountToken: false
