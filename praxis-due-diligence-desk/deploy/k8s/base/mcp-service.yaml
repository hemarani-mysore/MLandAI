apiVersion: v1
kind: Service
metadata:
  name: mcp
  namespace: praxis
  labels:
    app: praxis
    component: mcp
spec:
  selector:
    app: praxis
    component: mcp
  ports:
    - name: http
      port: 8001
      targetPort: http
